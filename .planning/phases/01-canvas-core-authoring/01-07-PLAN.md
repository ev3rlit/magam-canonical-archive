---
phase: 01-canvas-core-authoring
plan: 07
type: execute
wave: 7
depends_on:
  - 01-01
  - 01-03
files_modified:
  - app/components/editor/WorkspaceClient.tsx
  - app/components/editor/WorkspaceClient.test.tsx
  - app/hooks/useFileSync.ts
  - app/ws/methods.ts
  - app/ws/methods.test.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - Creating a new document must produce a real editable canvas document, not only a client-side placeholder tab.
    - The first create mutation on a new document must succeed without ENOENT or fake draft-version mismatches.
  artifacts:
    - path: app/components/editor/WorkspaceClient.tsx
      provides: new-document creation flow and initial workspace hydration.
    - path: app/hooks/useFileSync.ts
      provides: mutation parameter construction and version contract enforcement.
    - path: app/ws/methods.ts
      provides: node.create file resolution and mutation contract.
  key_links:
    - from: app/components/editor/WorkspaceClient.tsx
      to: app/ws/methods.ts
      via: first-mutation filePath and sourceVersion contract
      pattern: draftDocuments|createNode|ensureBaseVersion
---

<objective>
Close the new-document creation gap so an untitled canvas document is materialized into a real workspace file before authoring begins.

Purpose: Remove the client-only draft/document split that makes the first shape creation fail on a fresh untitled canvas.
Output: New-document materialization, valid sourceVersion hydration, and regression coverage for first-create success.
</objective>

<execution_context>
@/Users/danghamo/Documents/gituhb/magam-feature-database-first-canvas-platform/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danghamo/Documents/gituhb/magam-feature-database-first-canvas-platform/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-canvas-core-authoring/01-UAT.md
@.planning/debug/01-draft-document-node-create-enoent.md
@app/components/editor/WorkspaceClient.tsx
@app/hooks/useFileSync.ts
@app/ws/methods.ts
@app/ws/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression coverage for fresh untitled document materialization and first create mutation</name>
  <files>app/components/editor/WorkspaceClient.test.tsx, app/ws/methods.test.ts</files>
  <read_first>
app/components/editor/WorkspaceClient.test.tsx
app/ws/methods.test.ts
app/components/editor/WorkspaceClient.tsx
app/hooks/useFileSync.ts
app/ws/methods.ts
.planning/phases/01-canvas-core-authoring/01-UAT.md
  </read_first>
  <action>Add targeted coverage for the exact failing path reported in UAT. Cover creating an untitled document, materializing the backing `.graph.tsx` file or equivalent server-side document contract before the first edit, hydrating a real sourceVersion instead of `draft:empty-canvas`, and successfully executing the first `node.create` against the new document. Do not rely only on pre-existing fixture files.</action>
  <verify>
    <automated>bun test app/components/editor/WorkspaceClient.test.tsx app/ws/methods.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `app/components/editor/WorkspaceClient.test.tsx` covers untitled document create before first edit.
- `app/ws/methods.test.ts` covers the resulting file contract used by `node.create`.
- The regression proves the first create mutation does not fail with `ENOENT`.
  </acceptance_criteria>
  <done>The failing new-document path is captured before implementation changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Materialize untitled documents before edit RPCs and align the version contract</name>
  <files>app/components/editor/WorkspaceClient.tsx, app/hooks/useFileSync.ts, app/ws/methods.ts</files>
  <read_first>
app/components/editor/WorkspaceClient.tsx
app/hooks/useFileSync.ts
app/ws/methods.ts
app/ws/server.ts
.planning/debug/01-draft-document-node-create-enoent.md
  </read_first>
  <action>Replace the client-only draft placeholder path with a real document materialization flow that writes an empty canvas document into the active workspace root before authoring begins, or otherwise introduces an atomic create-document contract that produces a real file and sourceVersion before `node.create` runs. Ensure the filePath handed to the WS mutation layer matches the workspace root actually watched by the server, and remove any fake draft-version path that allows a mutation to proceed against a non-existent file. Keep the no-modal, immediate-open UX from Phase 01.</action>
  <verify>
    <automated>bun test app/components/editor/WorkspaceClient.test.tsx app/ws/methods.test.ts</automated>
  </verify>
  <acceptance_criteria>
- New untitled documents are backed by a real workspace file before the first edit mutation.
- `useFileSync` no longer treats a non-existent draft document as a valid editable file contract.
- `node.create` succeeds on a newly created untitled document without `ENOENT`.
  </acceptance_criteria>
  <done>Fresh canvas documents can be created and immediately edited through the normal mutation pipeline.</done>
</task>

</tasks>

<verification>
Run `bun test app/components/editor/WorkspaceClient.test.tsx app/ws/methods.test.ts`.
</verification>

<success_criteria>
The untitled-document flow produces a real editable file, and the first shape creation on that document succeeds through the same mutation path as existing workspace files.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canvas-core-authoring/01-07-SUMMARY.md`
</output>
