---
phase: 01-canvas-core-authoring
plan: 05
type: execute
wave: 5
depends_on:
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - app/features/editing/editability.ts
  - app/features/editing/commands.ts
  - app/features/editing/actionRoutingBridge/types.ts
  - app/features/editing/actionRoutingBridge/registry.ts
  - app/features/editing/actionRoutingBridge/registry.test.ts
  - app/components/GraphCanvas.tsx
  - app/components/GraphCanvas.drag.ts
  - app/components/GraphCanvas.test.tsx
  - app/components/editor/WorkspaceClient.tsx
  - app/store/graph.ts
  - app/store/graph.test.ts
  - app/features/canvas-ui-entrypoints/node-context-menu/buildNodeContextMenuModel.ts
  - app/processes/canvas-runtime/keyboard/keymap.ts
  - app/ws/methods.ts
  - app/ws/filePatcher.ts
  - app/ws/filePatcher.test.ts
  - e2e/canvas-core-authoring.spec.ts
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
  - AUTH-06
  - AUTH-07
must_haves:
  truths:
    - User can group and ungroup selections while preserving direct manipulation behavior.
    - User can enter and exit grouped content predictably on desktop and mobile shells.
    - User can preserve basic z-order and multi-selection-safe actions for grouped objects.
  artifacts:
    - path: app/features/editing/actionRoutingBridge/registry.ts
      provides: semantic commands for grouping, ungrouping, and z-order updates.
    - path: app/store/graph.ts
      provides: runtime state for group focus and multi-selection behavior.
    - path: app/ws/filePatcher.ts
      provides: compatibility write-back for structural grouping changes.
  key_links:
    - from: app/components/GraphCanvas.tsx
      to: app/store/graph.ts
      via: group focus entry, exit, and selection transitions
      pattern: group|selection
    - from: app/features/editing/actionRoutingBridge/registry.ts
      to: app/ws/methods.ts
      via: structural mutation descriptors
      pattern: group|reparent|z-order
---

<objective>
Close the structural editing loop for multi-selection so grouping, ungrouping, z-order, and group entry all behave like first-class canvas actions.

Purpose: Finish the structural half of Phase 1 authoring and connect grouping behavior to the existing semantic-command and compatibility-patch path.
Output: Grouping command contracts, runtime group-focus behavior, z-order basics, and keyboard hooks for group workflows.
</objective>

<execution_context>
@/Users/danghamo/Documents/gituhb/magam-feature-database-first-canvas-platform/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danghamo/Documents/gituhb/magam-feature-database-first-canvas-platform/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canvas-core-authoring/01-CONTEXT.md
@.planning/phases/01-canvas-core-authoring/01-RESEARCH.md
@docs/milestones/R1-external-agent-beta/R1-P0-workspace-document-canvas-authoring-convergence/README.md
@app/features/editing/editability.ts
@app/features/editing/commands.ts
@app/features/editing/actionRoutingBridge/types.ts
@app/features/editing/actionRoutingBridge/registry.ts
@app/components/GraphCanvas.tsx
@app/store/graph.ts
@app/ws/methods.ts
@app/ws/filePatcher.ts

<interfaces>
From `app/features/editing/actionRoutingBridge/types.ts`:
```ts
export type MutationActionId =
  | 'node.update'
  | 'node.create'
  | 'node.delete'
  | 'node.reparent';
```

From `app/features/editing/commands.ts`:
```ts
export interface ReparentPayload {
  previous: { parentId: string | null };
  next: { parentId: string | null };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define grouping and z-order command contracts with targeted regression coverage</name>
  <files>app/features/editing/editability.ts, app/features/editing/commands.ts, app/features/editing/actionRoutingBridge/types.ts, app/features/editing/actionRoutingBridge/registry.test.ts, app/store/graph.test.ts, app/ws/filePatcher.test.ts, e2e/canvas-core-authoring.spec.ts</files>
  <read_first>
app/features/editing/editability.ts
app/features/editing/commands.ts
app/features/editing/actionRoutingBridge/types.ts
app/features/editing/actionRoutingBridge/registry.test.ts
app/store/graph.test.ts
app/ws/filePatcher.test.ts
.planning/phases/01-canvas-core-authoring/01-CONTEXT.md
  </read_first>
  <action>Introduce explicit Phase 1 structural command contracts before UI wiring. Extend the editability and command layers with group-create, ungroup, and z-order mutation shapes that still flow through the existing semantic-command boundary rather than ad-hoc node array mutation. Add regression tests for group and ungroup command normalization, z-order intent payloads, multi-selection safety, progressive `Escape` unwind from inner group focus back to group selection, and a browser flow named `canvas core authoring structure: group ungroup z-order`.</action>
  <verify>
    <automated>bun test app/features/editing/actionRoutingBridge/registry.test.ts app/store/graph.test.ts app/ws/filePatcher.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `app/features/editing/commands.ts` contains explicit grouping or z-order command payload types.
- `app/features/editing/actionRoutingBridge/types.ts` contains dispatch descriptors for the new structural mutations.
- `app/features/editing/actionRoutingBridge/registry.test.ts` contains grouping or z-order intent coverage.
- `e2e/canvas-core-authoring.spec.ts` contains `canvas core authoring structure: group ungroup z-order`.
- `bun test app/features/editing/actionRoutingBridge/registry.test.ts app/store/graph.test.ts app/ws/filePatcher.test.ts` exits with status `0`.
  </acceptance_criteria>
  <done>The structural editing contract is explicit, typed, and protected by tests before runtime and patcher wiring lands.</done>
</task>

<task type="auto">
  <name>Task 2: Implement grouping, group focus entry or exit, and basic z-order across runtime and patch layers</name>
  <files>app/features/editing/actionRoutingBridge/registry.ts, app/components/GraphCanvas.tsx, app/components/GraphCanvas.drag.ts, app/components/editor/WorkspaceClient.tsx, app/store/graph.ts, app/features/canvas-ui-entrypoints/node-context-menu/buildNodeContextMenuModel.ts, app/ws/methods.ts, app/ws/filePatcher.ts, e2e/canvas-core-authoring.spec.ts</files>
  <read_first>
app/features/editing/actionRoutingBridge/registry.ts
app/components/GraphCanvas.tsx
app/components/GraphCanvas.drag.ts
app/components/editor/WorkspaceClient.tsx
app/store/graph.ts
app/ws/methods.ts
app/ws/filePatcher.ts
docs/milestones/R1-external-agent-beta/R1-P0-mobile-full-editing-shell/README.md
.planning/phases/01-canvas-core-authoring/01-CONTEXT.md
  </read_first>
  <action>Wire the new structural commands through the existing runtime. In `GraphCanvas.tsx` and `graph.ts`, add explicit group-focus state so desktop can enter a group through repeat click or double click while mobile exposes an explicit enter-group action instead of accidental nesting. Implement group and ungroup dispatch in `registry.ts`, `WorkspaceClient.tsx`, `methods.ts`, and `filePatcher.ts` so structural edits preserve the compatibility patch path. Add basic z-order actions that operate only on the selected set and preserve multi-selection-safe action exposure. Do not build deep layer panels, CRDT ordering, or Phase 2 relation semantics.</action>
  <verify>
    <automated>bun test app/features/editing/actionRoutingBridge/registry.test.ts app/store/graph.test.ts app/ws/filePatcher.test.ts app/components/GraphCanvas.test.tsx && bun run test:e2e --grep "canvas core authoring structure: group ungroup z-order"</automated>
  </verify>
  <acceptance_criteria>
- `app/store/graph.ts` contains explicit group-focus or inner-group state instead of implicit selection-only tracking.
- `app/components/GraphCanvas.tsx` contains desktop group-entry behavior and an explicit mobile entry path.
- `app/ws/methods.ts` and `app/ws/filePatcher.ts` contain grouping or z-order mutation handling.
- `app/features/canvas-ui-entrypoints/node-context-menu/buildNodeContextMenuModel.ts` exposes grouping-related actions only when structural context is valid.
- `bun run test:e2e --grep "canvas core authoring structure: group ungroup z-order"` exits with status `0`.
  </acceptance_criteria>
  <done>Grouping, ungrouping, group entry, and basic z-order all work through the same mutation path as the rest of Phase 1 editing.</done>
</task>

<task type="auto">
  <name>Task 3: Finish keyboard and multi-selection integration for structural editing</name>
  <files>app/processes/canvas-runtime/keyboard/keymap.ts, app/components/GraphCanvas.tsx, app/components/GraphCanvas.test.tsx, app/store/graph.ts, e2e/canvas-core-authoring.spec.ts</files>
  <read_first>
app/processes/canvas-runtime/keyboard/keymap.ts
app/components/GraphCanvas.tsx
app/components/GraphCanvas.test.tsx
app/store/graph.ts
.planning/phases/01-canvas-core-authoring/01-CONTEXT.md
  </read_first>
  <action>Complete the structural productivity loop by wiring group and ungroup shortcuts into the existing keyboard boundary and multi-selection state. Keep shortcut handling consistent with Plan `01-04`, preserve progressive `Escape` unwind, and ensure multi-selection floating or context actions only expose operations that are safe for the entire selection. Reuse the same group-focus state from Task 2; do not add a second selection model.</action>
  <verify>
    <automated>bun test app/components/GraphCanvas.test.tsx app/store/graph.test.ts app/features/editing/actionRoutingBridge/registry.test.ts && bun run test:e2e --grep "canvas core authoring structure: group ungroup z-order"</automated>
  </verify>
  <acceptance_criteria>
- `app/processes/canvas-runtime/keyboard/keymap.ts` contains group and ungroup bindings.
- `app/components/GraphCanvas.test.tsx` contains multi-selection or group-entry assertions.
- `app/store/graph.ts` continues to drive both selection and group-focus behavior.
- `bun run test:e2e --grep "canvas core authoring structure: group ungroup z-order"` exits with status `0`.
  </acceptance_criteria>
  <done>Structural editing is reachable from both direct manipulation and keyboard workflows, with multi-selection behavior staying coherent.</done>
</task>

</tasks>

<verification>
Run `bun test app/features/editing/actionRoutingBridge/registry.test.ts app/store/graph.test.ts app/ws/filePatcher.test.ts app/components/GraphCanvas.test.tsx`.
Run `bun run test:e2e --grep "canvas core authoring structure: group ungroup z-order"`.
</verification>

<success_criteria>
Users can group, ungroup, enter groups, exit groups, and preserve basic z-order behavior without breaking the existing selection, drag, or keyboard model.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canvas-core-authoring/01-05-SUMMARY.md`
</output>
