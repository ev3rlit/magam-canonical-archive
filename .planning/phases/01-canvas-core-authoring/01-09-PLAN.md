---
phase: 01-canvas-core-authoring
plan: 09
type: execute
wave: 8
depends_on:
  - 01-03
  - 01-06
files_modified:
  - app/components/nodes/renderableContent.tsx
  - app/components/nodes/renderableContent.test.tsx
  - app/components/nodes/ShapeNode.tsx
  - app/components/GraphCanvas.test.tsx
  - app/features/render/parseRenderGraph.ts
  - app/features/render/parseRenderGraph.test.ts
  - app/ws/filePatcher.ts
  - app/ws/filePatcher.test.ts
  - e2e/canvas-core-authoring.spec.ts
autonomous: true
gap_closure: true
requirements:
  - BODY-01
  - BODY-02
must_haves:
  truths:
    - Single-selected shape objects can enter markdown-first body editing through the same desktop and narrow/mobile entry rules used by other Phase 1 content nodes.
    - Editing content inside a shape persists through the canonical TSX patch path without collapsing the shape shell into a markdown-only node after reload.
    - Shape body editing keeps the existing selection and contextual-surface suppression contract instead of introducing a second body-edit state machine.
  artifacts:
    - path: app/components/nodes/renderableContent.tsx
      provides: shared body-edit session resolution for shape nodes with markdown-child source fallback.
    - path: app/components/nodes/ShapeNode.tsx
      provides: visible shape-shell read/edit rendering and explicit narrow/mobile body-entry affordance.
    - path: app/features/render/parseRenderGraph.ts
      provides: shape-shell parsing that preserves `shape` node type while carrying markdown child content.
    - path: app/ws/filePatcher.ts
      provides: shape content persistence through markdown child creation and update instead of label-only fallback.
  key_links:
    - from: app/components/nodes/renderableContent.tsx
      to: app/components/nodes/ShapeNode.tsx
      via: shared shape body session and markdown-first draft resolution
      pattern: resolveBodyEditSession|graph-markdown|markdown-wysiwyg
    - from: app/components/GraphCanvas.tsx
      to: app/components/nodes/ShapeNode.tsx
      via: single-selection Enter and double-click body-entry routing
      pattern: resolveNodeDoubleClickDecision|resolveSelectionBodyEditSession|Enter
    - from: app/ws/filePatcher.ts
      to: app/features/render/parseRenderGraph.ts
      via: Shape markdown child persistence must round-trip back into a `shape` node
      pattern: Shape|Markdown|type: 'shape'
---

<objective>
Close the remaining Phase 01 body-edit gap by making shape objects use the shared markdown-first content flow without losing their shape shell.

Purpose: Finish BODY-01 and BODY-02 at the actual shape-shell boundary so Phase 01 can honestly claim that every Phase 1 object is an editable container.
Output: Shape body-entry support, round-trip persistence that keeps shape nodes as shape nodes, and regression coverage across unit and browser layers.
</objective>

<execution_context>
@/Users/danghamo/Documents/gituhb/magam-feature-database-first-canvas-platform/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danghamo/Documents/gituhb/magam-feature-database-first-canvas-platform/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canvas-core-authoring/01-CONTEXT.md
@.planning/phases/01-canvas-core-authoring/01-RESEARCH.md
@.planning/phases/01-canvas-core-authoring/01-VERIFICATION.md
@.planning/phases/01-canvas-core-authoring/01-06-SUMMARY.md
@app/components/nodes/renderableContent.tsx
@app/components/nodes/ShapeNode.tsx
@app/components/GraphCanvas.tsx
@app/features/render/parseRenderGraph.ts
@app/ws/filePatcher.ts

<interfaces>
From `app/components/nodes/renderableContent.tsx`:
```ts
export function resolveBodyEditSession(
  node: BodyEditNodeLike | null | undefined,
): { nodeId: string; initialDraft: string; mode: TextEditMode } | null;
```

From `app/components/GraphCanvas.tsx`:
```ts
export function resolveNodeDoubleClickDecision(input: {
  node: BodyEditableFlowNode;
  selectedNodeIds: string[];
  nodes: CanvasDismissNode[];
}): GraphCanvasNodeDoubleClickDecision;

export function resolveSelectionBodyEditSession(input: {
  selectedNodeIds: string[];
  nodes: BodyEditableFlowNode[];
}): ReturnType<typeof resolveBodyEditSession>;
```

From `app/ws/filePatcher.ts`:
```ts
export async function patchNodeContent(filePath: string, nodeId: string, content: string): Promise<void>;
export async function patchNodeCreate(filePath: string, input: CreateNodeInput): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression coverage for shape markdown-first body entry and shape-shell round trips</name>
  <files>app/components/nodes/renderableContent.test.tsx, app/components/GraphCanvas.test.tsx, app/features/render/parseRenderGraph.test.ts, app/ws/filePatcher.test.ts, e2e/canvas-core-authoring.spec.ts</files>
  <read_first>
app/components/nodes/renderableContent.test.tsx
app/components/GraphCanvas.test.tsx
app/features/render/parseRenderGraph.test.ts
app/ws/filePatcher.test.ts
e2e/canvas-core-authoring.spec.ts
app/components/nodes/renderableContent.tsx
app/components/nodes/ShapeNode.tsx
app/features/render/parseRenderGraph.ts
app/ws/filePatcher.ts
.planning/phases/01-canvas-core-authoring/01-VERIFICATION.md
  </read_first>
  <action>Add targeted regressions that fail against the current BODY-01/BODY-02 gap. In `app/components/nodes/renderableContent.test.tsx`, replace the current `shape -> null` expectation with coverage that `resolveBodyEditSession` returns `{ nodeId, initialDraft, mode: 'markdown-wysiwyg' }` for `type: 'shape'`, preferring the first `graph-markdown` child `content` string over `label` and falling back to `label` only when no markdown child exists. In `app/components/GraphCanvas.test.tsx`, add a single-selection shape case proving `resolveNodeDoubleClickDecision` and `resolveSelectionBodyEditSession` start body editing for shapes while preserving the existing group-focus-first behavior. In `app/features/render/parseRenderGraph.test.ts`, add a `Shape` fixture with a nested `graph-markdown` child and assert the parsed node stays `type: 'shape'`, keeps its shell props (`type`, size, colors), and still exposes content-editable metadata. In `app/ws/filePatcher.test.ts`, add create/update coverage asserting `patchNodeCreate` and `patchNodeContent` write `<Markdown>{\`...\`}</Markdown>` inside `Shape` elements instead of plain text children. Extend `e2e/canvas-core-authoring.spec.ts` so the existing `canvas core authoring body: markdown first editing` flow includes a shape object entering body edit with desktop (`Enter` or double click) and narrow/mobile explicit `Edit` affordance, committing content, and reopening without `EDIT_REJECTED`.</action>
  <verify>
    <automated>bun test app/components/nodes/renderableContent.test.tsx app/components/GraphCanvas.test.tsx app/features/render/parseRenderGraph.test.ts app/ws/filePatcher.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `app/components/nodes/renderableContent.test.tsx` contains a shape expectation with `mode: 'markdown-wysiwyg'`.
- `app/components/GraphCanvas.test.tsx` contains a shape body-entry assertion referencing `resolveNodeDoubleClickDecision` or `resolveSelectionBodyEditSession`.
- `app/features/render/parseRenderGraph.test.ts` contains a case asserting a markdown-backed `Shape` still parses as `type: 'shape'`.
- `app/ws/filePatcher.test.ts` contains `Shape` plus `<Markdown>{\`` assertions for content persistence.
- `e2e/canvas-core-authoring.spec.ts` contains shape body-entry steps inside `canvas core authoring body: markdown first editing`.
- `bun test app/components/nodes/renderableContent.test.tsx app/components/GraphCanvas.test.tsx app/features/render/parseRenderGraph.test.ts app/ws/filePatcher.test.ts` exits with status `0`.
  </acceptance_criteria>
  <done>The remaining shape gap is pinned by focused regressions before renderer and persistence changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Implement shape-shell markdown body entry without collapsing shapes into markdown nodes</name>
  <files>app/components/nodes/renderableContent.tsx, app/components/nodes/ShapeNode.tsx, app/features/render/parseRenderGraph.ts, app/ws/filePatcher.ts, e2e/canvas-core-authoring.spec.ts</files>
  <read_first>
app/components/nodes/renderableContent.tsx
app/components/nodes/ShapeNode.tsx
app/components/GraphCanvas.tsx
app/features/render/parseRenderGraph.ts
app/ws/filePatcher.ts
app/components/nodes/TextNode.tsx
app/components/nodes/StickyNode.tsx
app/components/nodes/MarkdownNode.tsx
.planning/phases/01-canvas-core-authoring/01-VERIFICATION.md
.planning/phases/01-canvas-core-authoring/01-06-SUMMARY.md
  </read_first>
  <action>Extend the shared body-session contract to shape nodes instead of creating a shape-only editor path. In `app/components/nodes/renderableContent.tsx`, make `resolveBodyEditSession` accept `type: 'shape'` and compute `initialDraft` from the first `graph-markdown` child `content`, falling back to `label` only when no markdown child exists. In `app/components/nodes/ShapeNode.tsx`, mirror the existing Text/Sticky body-entry pattern: read `activeTextEditNodeId`, `textEditDraft`, `startTextEditSession`, `updateTextEditDraft`, `requestTextEditCommit`, and `requestTextEditCancel` from the graph store; show the narrow/coarse `Edit` affordance when selected and not already editing; render a markdown-first textarea editor for active shape sessions; and keep non-edit mode rendering through `renderNodeContent` so the shape shell, geometry, ports, and line/triangle variants stay intact. Preserve the existing generic `GraphCanvas` body-entry path rather than adding shape-specific keyboard or double-click handlers unless a test proves a generic path hole. In `app/features/render/parseRenderGraph.ts`, stop promoting `graph-node`/`Shape` elements with nested `graph-markdown` or `graph-image` children into `type: 'markdown'`; keep them `type: 'shape'`, preserve `size`, `type`, and style props, and still carry parsed children so `ShapeNode` renders markdown content inside the shell after reload. In `app/ws/filePatcher.ts`, treat tagName `Shape` as a markdown-source body carrier alongside `Text` and `Sticky`: `patchNodeContent` must upsert a `Markdown` child while still updating the visible `label`, and `patchNodeCreate` for `type: 'shape' | 'rectangle' | 'ellipse' | 'diamond' | 'line'` with string `content` must emit a `Shape` element containing `<Markdown>{\`...\`}</Markdown>` instead of plain text children. Do not introduce Phase 2 multi-block composition, WYSIWYG formatting, or a separate shape-only draft state.</action>
  <verify>
    <automated>bun test app/components/nodes/renderableContent.test.tsx app/components/GraphCanvas.test.tsx app/features/render/parseRenderGraph.test.ts app/ws/filePatcher.test.ts && MAGAM_WS_PORT=3012 NEXT_PUBLIC_MAGAM_WS_PORT=3012 bun run test:e2e --grep "canvas core authoring body: markdown first editing"</automated>
  </verify>
  <acceptance_criteria>
- `app/components/nodes/renderableContent.tsx` includes `'shape'` in the shared body-session resolver and reads markdown child content before `label`.
- `app/components/nodes/ShapeNode.tsx` contains the graph-store text-edit session hooks and an `Edit content` affordance or active textarea path.
- `app/features/render/parseRenderGraph.ts` no longer maps markdown-backed `Shape` elements to `type: 'markdown'`.
- `app/ws/filePatcher.ts` treats `tagName === 'Shape'` as a markdown-source body carrier for content create/update.
- `MAGAM_WS_PORT=3012 NEXT_PUBLIC_MAGAM_WS_PORT=3012 bun run test:e2e --grep "canvas core authoring body: markdown first editing"` exits with status `0`.
  </acceptance_criteria>
  <done>Shape nodes participate in the same markdown-first body-edit lifecycle as the rest of Phase 1 without losing their shell or persistence contract.</done>
</task>

</tasks>

<verification>
Run `bun test app/components/nodes/renderableContent.test.tsx app/components/GraphCanvas.test.tsx app/features/render/parseRenderGraph.test.ts app/ws/filePatcher.test.ts`.
Run `MAGAM_WS_PORT=3012 NEXT_PUBLIC_MAGAM_WS_PORT=3012 bun run test:e2e --grep "canvas core authoring body: markdown first editing"`.
</verification>

<success_criteria>
Selecting a single shape and invoking body edit enters the shared markdown-first session, committed content persists as a markdown child inside the `Shape` TSX source, and reload keeps the object rendered as a shape shell rather than degrading into a markdown-only node.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canvas-core-authoring/01-09-SUMMARY.md`
</output>
