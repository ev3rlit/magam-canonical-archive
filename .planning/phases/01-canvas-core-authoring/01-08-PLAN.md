---
phase: 01-canvas-core-authoring
plan: 08
type: execute
wave: 7
depends_on:
  - 01-04
  - 01-05
files_modified:
  - app/components/GraphCanvas.tsx
  - app/components/GraphCanvas.test.tsx
  - app/features/editing/actionRoutingBridge/registry.ts
  - app/features/editing/actionRoutingBridge/registry.test.ts
  - app/features/overlay-host/positioning.test.ts
  - e2e/canvas-core-authoring.spec.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - The floating selection controls must remain visually attached to the selected object under pan and zoom.
    - Grouping must leave the user in a stable structural selection state so group focus can be entered immediately afterward.
  artifacts:
    - path: app/components/GraphCanvas.tsx
      provides: selection-bounds anchor registration and group-focus transition rules.
    - path: app/features/editing/actionRoutingBridge/registry.ts
      provides: post-group dispatch sequencing and structural selection handoff.
    - path: e2e/canvas-core-authoring.spec.ts
      provides: browser-level coverage for grouping and contextual surfaces.
  key_links:
    - from: app/components/GraphCanvas.tsx
      to: app/features/overlay-host/positioning.test.ts
      via: selection-bounds anchor coordinate contract
      pattern: selection-floating-menu|selection-bounds|viewport
    - from: app/features/editing/actionRoutingBridge/registry.ts
      to: app/components/GraphCanvas.tsx
      via: grouping success must preserve full-group selection before enter-group
      pattern: selection.group|select-node-group|resolveGroupFocusEntry
---

<objective>
Repair the contextual selection UX so floating controls anchor correctly and fresh grouping preserves the structural selection needed for group focus.

Purpose: Close the remaining user-facing selection gaps in Phase 01 after contextual actions and structural grouping landed.
Output: Correct floating-menu anchoring, stable post-group selection behavior, and browser coverage that exercises fresh grouping instead of only pre-grouped fixtures.
</objective>

<execution_context>
@/Users/danghamo/Documents/gituhb/magam-feature-database-first-canvas-platform/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danghamo/Documents/gituhb/magam-feature-database-first-canvas-platform/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-canvas-core-authoring/01-UAT.md
@.planning/debug/01-selection-floating-menu-anchor-space.md
@.planning/debug/01-grouping-selection-does-not-promote-group.md
@app/components/GraphCanvas.tsx
@app/features/editing/actionRoutingBridge/registry.ts
@app/features/overlay-host/positioning.ts
@e2e/canvas-core-authoring.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression coverage for anchor-space correctness and fresh-group selection continuity</name>
  <files>app/components/GraphCanvas.test.tsx, app/features/editing/actionRoutingBridge/registry.test.ts, app/features/overlay-host/positioning.test.ts, e2e/canvas-core-authoring.spec.ts</files>
  <read_first>
app/components/GraphCanvas.test.tsx
app/features/editing/actionRoutingBridge/registry.test.ts
app/features/overlay-host/positioning.test.ts
e2e/canvas-core-authoring.spec.ts
app/components/GraphCanvas.tsx
app/features/editing/actionRoutingBridge/registry.ts
  </read_first>
  <action>Add targeted tests for the two UAT gaps. Cover selection-floating-menu anchoring with a non-default viewport transform so the overlay and selection shell stay aligned, and add browser coverage for grouping a fresh multi-selection and then entering group focus. Do not rely only on pre-grouped fixtures or zero-pan/zoom assumptions.</action>
  <verify>
    <automated>bun test app/components/GraphCanvas.test.tsx app/features/editing/actionRoutingBridge/registry.test.ts app/features/overlay-host/positioning.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `app/components/GraphCanvas.test.tsx` or `positioning.test.ts` asserts selection-floating anchors use screen-space coordinates.
- `e2e/canvas-core-authoring.spec.ts` covers grouping a fresh multi-selection before group-focus entry.
- The new coverage would fail against the current UAT-reported behavior.
  </acceptance_criteria>
  <done>The two remaining contextual selection gaps are locked with regression coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Correct selection anchor coordinates and preserve structural selection after grouping</name>
  <files>app/components/GraphCanvas.tsx, app/features/editing/actionRoutingBridge/registry.ts, app/components/GraphCanvas.test.tsx, app/features/editing/actionRoutingBridge/registry.test.ts, app/features/overlay-host/positioning.test.ts, e2e/canvas-core-authoring.spec.ts</files>
  <read_first>
app/components/GraphCanvas.tsx
app/features/editing/actionRoutingBridge/registry.ts
app/features/overlay-host/positioning.ts
app/components/GraphCanvas.test.tsx
.planning/debug/01-selection-floating-menu-anchor-space.md
.planning/debug/01-grouping-selection-does-not-promote-group.md
  </read_first>
  <action>Make `selection-bounds` anchors use viewport-transformed screen coordinates so the hosted floating menu tracks the same geometry as the selection shell. Then extend the `selection.group` success path so grouping immediately preserves or restores full-group structural selection before the user attempts to enter group focus. Reuse the existing `select-node-group` runtime action or another single-source selection handoff instead of adding a second parallel selection model.</action>
  <verify>
    <automated>bun test app/components/GraphCanvas.test.tsx app/features/editing/actionRoutingBridge/registry.test.ts app/features/overlay-host/positioning.test.ts && bun run test:e2e --grep "canvas core authoring structure: group ungroup z-order"</automated>
  </verify>
  <acceptance_criteria>
- The floating selection controls stay anchored to the selected object after pan or zoom.
- After grouping fresh multi-selection, the grouped set remains structurally selected and group focus can be entered without reselecting manually.
- The browser structure flow covers fresh grouping and still passes existing ungroup/z-order assertions.
  </acceptance_criteria>
  <done>Contextual selection surfaces and structural grouping feel stable and predictable in live canvas use.</done>
</task>

</tasks>

<verification>
Run `bun test app/components/GraphCanvas.test.tsx app/features/editing/actionRoutingBridge/registry.test.ts app/features/overlay-host/positioning.test.ts`.
Run `bun run test:e2e --grep "canvas core authoring structure: group ungroup z-order"`.
</verification>

<success_criteria>
The floating menu remains visually attached to the active selection, and grouping no longer drops the user out of the structural-selection path needed for group focus.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canvas-core-authoring/01-08-SUMMARY.md`
</output>
