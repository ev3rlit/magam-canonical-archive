---
phase: 01-canvas-core-authoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/editor/WorkspaceClient.tsx
  - app/components/editor/WorkspaceClient.test.tsx
  - app/components/ui/Header.tsx
  - app/components/ui/Sidebar.tsx
  - app/components/ui/QuickOpenDialog.tsx
  - app/components/ui/TabBar.tsx
  - app/store/graph.ts
  - app/store/graph.test.ts
  - e2e/canvas-core-authoring.spec.ts
autonomous: true
requirements:
  - AUTH-01
must_haves:
  truths:
    - User resumes into the last active document instead of landing in a setup-first shell.
    - User can create a new document and land on an empty canvas immediately.
    - Workspace and document identity stay visible while editing without taking over the canvas.
  artifacts:
    - path: app/components/editor/WorkspaceClient.tsx
      provides: entry orchestration for resume, open, switch, and empty-canvas boot.
    - path: app/store/graph.ts
      provides: persisted workspace and last-active document session state.
    - path: e2e/canvas-core-authoring.spec.ts
      provides: browser regression coverage for entry and document switching.
  key_links:
    - from: app/components/editor/WorkspaceClient.tsx
      to: app/store/graph.ts
      via: boot-time resume and tab activation
      pattern: openTab|activateTab|lastActive
    - from: app/components/ui/QuickOpenDialog.tsx
      to: app/components/editor/WorkspaceClient.tsx
      via: command-driven document creation and switching
      pattern: onRunCommand|onOpenFile
---

<objective>
Converge workspace, document, and canvas entry so the app always feels like a canvas product first instead of a file browser with a canvas attached.

Purpose: Close the Phase 1 entry gap by making resume, document switching, and empty-canvas creation immediate and lightweight.
Output: Persisted last-active document behavior, lightweight workspace/document chrome, and automated entry-flow coverage.
</objective>

<execution_context>
@/Users/danghamo/Documents/gituhb/magam-feature-database-first-canvas-platform/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danghamo/Documents/gituhb/magam-feature-database-first-canvas-platform/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canvas-core-authoring/01-CONTEXT.md
@.planning/phases/01-canvas-core-authoring/01-RESEARCH.md
@.planning/phases/01-canvas-core-authoring/01-VALIDATION.md
@docs/milestones/R1-external-agent-beta/R1-P0-workspace-document-canvas-authoring-convergence/README.md
@app/components/editor/WorkspaceClient.tsx
@app/store/graph.ts
@app/components/ui/Header.tsx
@app/components/ui/Sidebar.tsx
@app/components/ui/QuickOpenDialog.tsx
@app/components/ui/TabBar.tsx

<interfaces>
From `app/store/graph.ts`:
```ts
export interface TabState {
  tabId: string;
  pageId: string;
  title: string;
  dirty: boolean;
  lastViewport: TabViewportState | null;
  lastSelection: TabSelectionState | null;
}

export type OpenTabResult =
  | { status: 'activated' | 'opened'; tabId: string }
  | { status: 'blocked'; replaceTabId: string };
```

From `app/components/ui/QuickOpenDialog.tsx`:
```ts
export interface QuickOpenCommand {
  id: string;
  label: string;
  hint?: string;
  keywords?: string[];
  disabled?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add entry-flow regression coverage and the Phase 1 browser scaffold</name>
  <files>app/components/editor/WorkspaceClient.test.tsx, app/store/graph.test.ts, e2e/canvas-core-authoring.spec.ts</files>
  <read_first>
app/components/editor/WorkspaceClient.test.tsx
app/store/graph.test.ts
app/components/editor/WorkspaceClient.tsx
app/store/graph.ts
.planning/phases/01-canvas-core-authoring/01-CONTEXT.md
.planning/phases/01-canvas-core-authoring/01-VALIDATION.md
  </read_first>
  <action>Add focused tests for Phase 1 entry convergence. In `app/components/editor/WorkspaceClient.test.tsx`, cover boot-time resume into the last active document, inline new-document creation that opens an empty canvas immediately, and fast document switching via Quick Open without a blocking naming modal. In `app/store/graph.test.ts`, add state coverage for persisted last-active document metadata and tab restoration. Create `e2e/canvas-core-authoring.spec.ts` with named Playwright tests for `canvas core authoring entry: resumes last active document` and `canvas core authoring entry: creates a document into an empty canvas`. Keep the tests inside the current `/api/files` and `/api/file-tree` shell; do not introduce Phase 2 body-block assertions.</action>
  <verify>
    <automated>bun test app/components/editor/WorkspaceClient.test.tsx app/store/graph.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `app/components/editor/WorkspaceClient.test.tsx` contains `describe('WorkspaceClient document entry convergence'`.
- `app/store/graph.test.ts` contains `describe('document session persistence'`.
- `e2e/canvas-core-authoring.spec.ts` contains `canvas core authoring entry: resumes last active document`.
- `bun test app/components/editor/WorkspaceClient.test.tsx app/store/graph.test.ts` exits with status `0`.
  </acceptance_criteria>
  <done>Regression coverage exists for resume, open, switch, and empty-canvas creation before implementation changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Implement last-active resume, lightweight document context, and inline empty-canvas creation</name>
  <files>app/components/editor/WorkspaceClient.tsx, app/components/ui/Header.tsx, app/components/ui/Sidebar.tsx, app/components/ui/QuickOpenDialog.tsx, app/components/ui/TabBar.tsx, app/store/graph.ts, e2e/canvas-core-authoring.spec.ts</files>
  <read_first>
app/components/editor/WorkspaceClient.tsx
app/components/ui/Header.tsx
app/components/ui/Sidebar.tsx
app/components/ui/QuickOpenDialog.tsx
app/components/ui/TabBar.tsx
app/store/graph.ts
docs/milestones/R1-external-agent-beta/R1-P0-workspace-document-canvas-authoring-convergence/README.md
.planning/phases/01-canvas-core-authoring/01-CONTEXT.md
  </read_first>
  <action>Use the existing `WorkspaceClient` plus `useGraphStore` tab model as the single entry seam. Add persisted last-active document state keyed to the current workspace/file tree source, hydrate that state on boot, and reopen the last active document before showing an idle shell. Add a Quick Open command for `new document` that inserts a fresh untitled document entry and opens it straight into an empty canvas without a naming gate. Keep workspace and document identity visible through lightweight header/tab/sidebar copy, with inline naming affordance after entry rather than before entry. Preserve the current `/api/files` and `/api/file-tree` transport and AST patch compatibility path; do not introduce a second shell or a modal onboarding flow.</action>
  <verify>
    <automated>bun test app/components/editor/WorkspaceClient.test.tsx app/store/graph.test.ts && bun run test:e2e --grep "canvas core authoring entry"</automated>
  </verify>
  <acceptance_criteria>
- `app/store/graph.ts` contains a persisted last-active document/session field with the string `lastActive`.
- `app/components/editor/WorkspaceClient.tsx` contains boot-time resume logic that reopens a document through `openTab` or `activateTab`.
- `app/components/ui/QuickOpenDialog.tsx` or `app/components/editor/WorkspaceClient.tsx` contains a command id string for `new-document`.
- `app/components/ui/Header.tsx` still renders lightweight editing context and does not introduce a blocking onboarding state.
- `bun run test:e2e --grep "canvas core authoring entry"` exits with status `0`.
  </acceptance_criteria>
  <done>Opening the app resumes directly into the last document, document switching stays lightweight, and creating a new document lands on an empty canvas immediately.</done>
</task>

</tasks>

<verification>
Run `bun test app/components/editor/WorkspaceClient.test.tsx app/store/graph.test.ts`.
Run `bun run test:e2e --grep "canvas core authoring entry"`.
</verification>

<success_criteria>
The primary app flow opens directly into a canvas document, new document creation is empty-canvas first, and workspace/document context remains visible without dominating the shell.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canvas-core-authoring/01-01-SUMMARY.md`
</output>
