---
phase: 01-canvas-core-authoring
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - libs/core/src/components/Shape.tsx
  - app/features/render/parseRenderGraph.ts
  - app/features/render/parseRenderGraph.test.ts
  - app/features/editing/createDefaults.ts
  - app/features/editing/actionRoutingBridge/registry.ts
  - app/features/editing/actionRoutingBridge/registry.test.ts
  - app/features/canvas-ui-entrypoints/canvas-toolbar/toolbarActions.ts
  - app/features/canvas-ui-entrypoints/pane-context-menu/paneMenuItems.ts
  - app/components/GraphCanvas.tsx
  - app/components/GraphCanvas.test.tsx
  - app/ws/filePatcher.ts
  - app/ws/filePatcher.test.ts
  - e2e/canvas-core-authoring.spec.ts
autonomous: true
requirements:
  - SHAP-01
must_haves:
  truths:
    - User can create rectangle, ellipse, diamond, text, arrow or line, and sticky objects from primary creation surfaces.
    - Text, markdown, and sticky creation enter editing immediately; geometry-first objects and arrow or line stay selected.
    - Creation defaults to one-shot mode, with repeat-create remaining deliberate rather than sticky-by-default.
  artifacts:
    - path: libs/core/src/components/Shape.tsx
      provides: core shape contract for the minimal Phase 1 geometry set.
    - path: app/features/render/parseRenderGraph.ts
      provides: parser support for the minimal shape set and creation metadata.
    - path: app/ws/filePatcher.ts
      provides: compatibility write-back for created objects.
  key_links:
    - from: app/features/canvas-ui-entrypoints/canvas-toolbar/toolbarActions.ts
      to: app/components/GraphCanvas.tsx
      via: create-mode selection and one-shot reset
      pattern: create|setEntrypointCreateMode
    - from: app/features/editing/actionRoutingBridge/registry.ts
      to: app/ws/filePatcher.ts
      via: node.create dispatch payloads
      pattern: node.create|placement
---

<objective>
Deliver the minimal Phase 1 shape set and make creation flows feel intentional, fast, and object-specific instead of generic.

Purpose: Unlock the core authoring loop by letting users create the right primitives from toolbar, pane, and contextual entry surfaces.
Output: Minimal shape contracts, creation defaults, one-shot create flows, and compatibility patch coverage for the full Phase 1 shape set.
</objective>

<execution_context>
@/Users/danghamo/Documents/gituhb/magam-feature-database-first-canvas-platform/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danghamo/Documents/gituhb/magam-feature-database-first-canvas-platform/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canvas-core-authoring/01-CONTEXT.md
@.planning/phases/01-canvas-core-authoring/01-RESEARCH.md
@docs/milestones/R1-external-agent-beta/R1-P0-workspace-document-canvas-authoring-convergence/README.md
@libs/core/src/components/Shape.tsx
@app/features/render/parseRenderGraph.ts
@app/features/editing/createDefaults.ts
@app/features/editing/actionRoutingBridge/registry.ts
@app/ws/filePatcher.ts

<interfaces>
From `libs/core/src/components/Shape.tsx`:
```ts
export interface ShapeProps {
  id?: string;
  type?: 'rectangle' | 'circle' | 'triangle' | string;
  x?: number;
  y?: number;
  anchor?: string;
  position?: AnchorPosition;
  gap?: number;
}
```

From `app/features/editing/commands.ts`:
```ts
export interface CreatePayload {
  nodeType: 'shape' | 'text' | 'markdown' | 'sticky' | 'sticker' | 'washi-tape' | 'image';
  placement:
    | { mode: 'canvas-absolute'; x: number; y: number }
    | { mode: 'mindmap-child'; parentId: string }
    | { mode: 'mindmap-sibling'; siblingOf: string; parentId: string | null };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend contracts and tests for the minimal Phase 1 shape set</name>
  <files>libs/core/src/components/Shape.tsx, app/features/render/parseRenderGraph.ts, app/features/render/parseRenderGraph.test.ts, app/features/editing/actionRoutingBridge/registry.test.ts, app/ws/filePatcher.test.ts, e2e/canvas-core-authoring.spec.ts</files>
  <read_first>
libs/core/src/components/Shape.tsx
app/features/render/parseRenderGraph.ts
app/features/render/parseRenderGraph.test.ts
app/features/editing/actionRoutingBridge/registry.ts
app/ws/filePatcher.ts
.planning/phases/01-canvas-core-authoring/01-CONTEXT.md
  </read_first>
  <action>Define the minimal Phase 1 shape contract in tests before implementation. Extend `ShapeProps` and parser expectations so the app can represent `rectangle`, `ellipse`, `diamond`, and minimal `line` or `arrow` geometry through the existing `graph-shape` path instead of inventing a separate authoring shell. Add parser, action-routing, file-patcher, and Playwright coverage for object-specific creation rules: click-or-drag for rectangle, ellipse, diamond, and sticky; click-create plus immediate edit for text and markdown; drag-defined endpoints for line or arrow. Keep Phase 1 connectors intentionally simple: start, end, basic style, and optional label only.</action>
  <verify>
    <automated>bun test app/features/render/parseRenderGraph.test.ts app/features/editing/actionRoutingBridge/registry.test.ts app/ws/filePatcher.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `app/features/render/parseRenderGraph.test.ts` contains assertions for `ellipse`, `diamond`, and `line` or `arrow`.
- `app/ws/filePatcher.test.ts` contains creation coverage for the minimal shape set.
- `e2e/canvas-core-authoring.spec.ts` contains `canvas core authoring create: minimal shape set`.
- `bun test app/features/render/parseRenderGraph.test.ts app/features/editing/actionRoutingBridge/registry.test.ts app/ws/filePatcher.test.ts` exits with status `0`.
  </acceptance_criteria>
  <done>The minimal shape set is encoded in executable tests and contracts before create-flow code changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Implement toolbar, pane, and compatibility-path creation flows for the minimal shape set</name>
  <files>libs/core/src/components/Shape.tsx, app/features/render/parseRenderGraph.ts, app/features/editing/createDefaults.ts, app/features/editing/actionRoutingBridge/registry.ts, app/features/canvas-ui-entrypoints/canvas-toolbar/toolbarActions.ts, app/features/canvas-ui-entrypoints/pane-context-menu/paneMenuItems.ts, app/components/GraphCanvas.tsx, app/ws/filePatcher.ts, e2e/canvas-core-authoring.spec.ts</files>
  <read_first>
libs/core/src/components/Shape.tsx
app/features/render/parseRenderGraph.ts
app/features/editing/createDefaults.ts
app/features/editing/actionRoutingBridge/registry.ts
app/features/canvas-ui-entrypoints/canvas-toolbar/toolbarActions.ts
app/features/canvas-ui-entrypoints/pane-context-menu/paneMenuItems.ts
app/components/GraphCanvas.tsx
app/ws/filePatcher.ts
.planning/phases/01-canvas-core-authoring/01-CONTEXT.md
  </read_first>
  <action>Implement the minimal shape set on the existing seams. Expand `ShapeProps`, parser normalization, create defaults, and compatibility patch generation so rectangle, ellipse, diamond, text, sticky, and simple line or arrow creation all dispatch through `node.create`. Update toolbar and pane create inventories to expose the full set while keeping one-shot mode the default; add deliberate repeat-create only through explicit toolbar state, not automatic persistence. In `GraphCanvas.tsx`, keep object-specific create rules: text, markdown, and sticky start editing immediately after creation; geometry-first objects and line or arrow stay selected. Do not introduce richer connector semantics, relation attachment, or multi-block body behavior in this plan.</action>
  <verify>
    <automated>bun test app/features/render/parseRenderGraph.test.ts app/features/editing/actionRoutingBridge/registry.test.ts app/ws/filePatcher.test.ts app/components/GraphCanvas.test.tsx && bun run test:e2e --grep "canvas core authoring create: minimal shape set"</automated>
  </verify>
  <acceptance_criteria>
- `app/features/editing/createDefaults.ts` contains defaults for every Phase 1 createable type exposed by the toolbar or pane menu.
- `app/features/canvas-ui-entrypoints/canvas-toolbar/toolbarActions.ts` exposes creation actions for rectangle, ellipse, diamond, text, arrow or line, and sticky.
- `app/features/canvas-ui-entrypoints/pane-context-menu/paneMenuItems.ts` exposes matching create entries for the empty pane surface.
- `app/ws/filePatcher.ts` can write back created nodes for the new shape types without bypassing `node.create`.
- `bun run test:e2e --grep "canvas core authoring create: minimal shape set"` exits with status `0`.
  </acceptance_criteria>
  <done>The minimal Phase 1 shape set can be created from the primary authoring surfaces with the locked one-shot and immediate-edit behaviors.</done>
</task>

</tasks>

<verification>
Run `bun test app/features/render/parseRenderGraph.test.ts app/features/editing/actionRoutingBridge/registry.test.ts app/ws/filePatcher.test.ts app/components/GraphCanvas.test.tsx`.
Run `bun run test:e2e --grep "canvas core authoring create: minimal shape set"`.
</verification>

<success_criteria>
Users can create the entire minimal Phase 1 shape set from the main creation flows, and the creation lifecycle respects object-specific behavior without introducing deferred connector or body scope.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canvas-core-authoring/01-03-SUMMARY.md`
</output>
