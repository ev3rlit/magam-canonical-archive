---
phase: 01-canvas-core-authoring
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/components/GraphCanvas.tsx
  - app/components/GraphCanvas.drag.ts
  - app/components/GraphCanvas.test.tsx
  - app/components/GraphCanvas.viewport.ts
  - app/components/GraphCanvas.viewport.test.ts
  - app/store/graph.ts
  - app/store/graph.test.ts
  - e2e/canvas-core-authoring.spec.ts
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
must_haves:
  truths:
    - User can pan and zoom smoothly with mouse, trackpad, and shortcuts.
    - User can marquee-select, multi-select, deselect, and drag selections predictably.
    - User can resize and rotate from visible handles with state preserved through selection changes.
  artifacts:
    - path: app/components/GraphCanvas.tsx
      provides: viewport, selection, drag, resize, and rotate host behavior.
    - path: app/components/GraphCanvas.drag.ts
      provides: gesture threshold and drag-stop policy for canvas manipulation.
    - path: app/components/GraphCanvas.viewport.ts
      provides: viewport persistence and restoration helpers.
  key_links:
    - from: app/components/GraphCanvas.tsx
      to: app/store/graph.ts
      via: selection, viewport, and text-edit state transitions
      pattern: setSelectedNodes|setActiveTabViewport|entrypointRuntime
    - from: app/components/GraphCanvas.tsx
      to: app/components/GraphCanvas.drag.ts
      via: drag threshold and commit policy
      pattern: shouldCommitDragStop|shouldHandlePaneCreate
---

<objective>
Establish the direct-manipulation baseline so pan, zoom, select, drag, resize, and rotate feel like a real canvas instead of a static graph viewer.

Purpose: Close the highest-frequency manipulation gaps before creation, menus, and body editing build on top of them.
Output: Viewport persistence, marquee/multi-selection, input-aware drag tuning, and visible resize/rotate affordances with automated coverage.
</objective>

<execution_context>
@/Users/danghamo/Documents/gituhb/magam-feature-database-first-canvas-platform/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danghamo/Documents/gituhb/magam-feature-database-first-canvas-platform/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canvas-core-authoring/01-CONTEXT.md
@.planning/phases/01-canvas-core-authoring/01-RESEARCH.md
@.planning/phases/01-canvas-core-authoring/01-VALIDATION.md
@docs/milestones/R1-external-agent-beta/R1-P0-workspace-document-canvas-authoring-convergence/README.md
@app/components/GraphCanvas.tsx
@app/components/GraphCanvas.drag.ts
@app/components/GraphCanvas.viewport.ts
@app/store/graph.ts

<interfaces>
From `app/store/graph.ts`:
```ts
export interface TabViewportState {
  x: number;
  y: number;
  zoom: number;
}

export interface TabSelectionState {
  nodeIds: string[];
  edgeIds: string[];
  updatedAt: number;
}
```

From `app/components/GraphCanvas.drag.ts`:
```ts
export type GraphCanvasCreateMode = CanvasEntrypointCreateMode;

export function shouldCommitDragStop(input: {
  origin?: { x: number; y: number };
  current: { x: number; y: number };
}): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add viewport and direct-manipulation regression coverage</name>
  <files>app/components/GraphCanvas.test.tsx, app/components/GraphCanvas.viewport.test.ts, app/store/graph.test.ts, e2e/canvas-core-authoring.spec.ts</files>
  <read_first>
app/components/GraphCanvas.test.tsx
app/components/GraphCanvas.viewport.test.ts
app/store/graph.test.ts
app/components/GraphCanvas.tsx
app/components/GraphCanvas.drag.ts
app/components/GraphCanvas.viewport.ts
.planning/phases/01-canvas-core-authoring/01-CONTEXT.md
  </read_first>
  <action>Extend automated coverage for Phase 1 manipulation rules. Add unit and component tests for viewport restore, marquee selection, additive multi-select, progressive deselect, input-aware drag thresholds, auto-pan policy, and visible resize/rotate shell state. Add Playwright coverage in `e2e/canvas-core-authoring.spec.ts` for a named test `canvas core authoring viewport: pan zoom select drag resize rotate`. Make the assertions match the locked Phase 1 behavior, including conservative touch thresholds and progressive unwind from edit to group to clear selection.</action>
  <verify>
    <automated>bun test app/components/GraphCanvas.test.tsx app/components/GraphCanvas.viewport.test.ts app/store/graph.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `app/components/GraphCanvas.test.tsx` contains `describe('GraphCanvas direct manipulation baseline'`.
- `app/components/GraphCanvas.viewport.test.ts` contains `restore`.
- `e2e/canvas-core-authoring.spec.ts` contains `canvas core authoring viewport: pan zoom select drag resize rotate`.
- `bun test app/components/GraphCanvas.test.tsx app/components/GraphCanvas.viewport.test.ts app/store/graph.test.ts` exits with status `0`.
  </acceptance_criteria>
  <done>Regression coverage exists for the complete viewport and selection baseline before host behavior changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Implement pan, zoom, marquee, and drag-selection baseline in GraphCanvas</name>
  <files>app/components/GraphCanvas.tsx, app/components/GraphCanvas.drag.ts, app/components/GraphCanvas.viewport.ts, app/store/graph.ts, e2e/canvas-core-authoring.spec.ts</files>
  <read_first>
app/components/GraphCanvas.tsx
app/components/GraphCanvas.drag.ts
app/components/GraphCanvas.viewport.ts
app/store/graph.ts
docs/milestones/R1-external-agent-beta/R1-P0-workspace-document-canvas-authoring-convergence/README.md
.planning/phases/01-canvas-core-authoring/01-CONTEXT.md
  </read_first>
  <action>Keep `GraphCanvas.tsx` as the host and strengthen the existing React Flow integration instead of adding a second interaction layer. Tune pan and zoom so wheel, trackpad, and shortcut inputs share the same viewport truth and persist through `TabViewportState`. Add marquee selection, additive multi-selection, and empty-canvas deselect rules that unwind progressively from body edit to inner-group focus to clear selection. Use `GraphCanvas.drag.ts` to introduce faster desktop drag thresholds and more conservative touch thresholds, and make auto-pan more willing during move/drag than during resize/rotate. Preserve existing overlay/runtime bindings.</action>
  <verify>
    <automated>bun test app/components/GraphCanvas.test.tsx app/components/GraphCanvas.viewport.test.ts app/store/graph.test.ts && bun run test:e2e --grep "canvas core authoring viewport"</automated>
  </verify>
  <acceptance_criteria>
- `app/components/GraphCanvas.tsx` contains selection or viewport handlers for marquee/deselect behavior.
- `app/components/GraphCanvas.drag.ts` contains input-aware threshold logic rather than a single drag threshold.
- `app/components/GraphCanvas.viewport.ts` continues to round-trip `TabViewportState`.
- `bun run test:e2e --grep "canvas core authoring viewport"` exits with status `0`.
  </acceptance_criteria>
  <done>User can pan, zoom, select, deselect, and drag on the canvas through one stable baseline across desktop and touch inputs.</done>
</task>

<task type="auto">
  <name>Task 3: Add visible resize and rotate affordances without breaking selection state</name>
  <files>app/components/GraphCanvas.tsx, app/components/GraphCanvas.drag.ts, app/store/graph.ts, app/components/GraphCanvas.test.tsx, e2e/canvas-core-authoring.spec.ts</files>
  <read_first>
app/components/GraphCanvas.tsx
app/components/GraphCanvas.drag.ts
app/store/graph.ts
app/components/GraphCanvas.test.tsx
.planning/phases/01-canvas-core-authoring/01-CONTEXT.md
  </read_first>
  <action>Implement visible resize and rotate handles on the current selection shell inside `GraphCanvas.tsx`. Keep resize/rotate as selection-level affordances, not inspector-only actions. Persist the resulting geometry through the existing semantic-command and graph-store flow, and ensure empty-canvas click and `Escape` preserve the locked progressive unwind order. Make auto-pan conservative while resize/rotate is active and do not allow resize/rotate handles to collide with marquee initiation.</action>
  <verify>
    <automated>bun test app/components/GraphCanvas.test.tsx app/components/GraphCanvas.viewport.test.ts app/store/graph.test.ts && bun run test:e2e --grep "canvas core authoring viewport"</automated>
  </verify>
  <acceptance_criteria>
- `app/components/GraphCanvas.tsx` contains explicit resize or rotate handle rendering code.
- `app/components/GraphCanvas.test.tsx` contains assertions for resize or rotate behavior.
- `app/store/graph.ts` still owns the resulting selection state after resize/rotate commits.
- `bun run test:e2e --grep "canvas core authoring viewport"` exits with status `0`.
  </acceptance_criteria>
  <done>Selected objects expose visible resize and rotate affordances, and those interactions preserve consistent selection and viewport behavior.</done>
</task>

</tasks>

<verification>
Run `bun test app/components/GraphCanvas.test.tsx app/components/GraphCanvas.viewport.test.ts app/store/graph.test.ts`.
Run `bun run test:e2e --grep "canvas core authoring viewport"`.
</verification>

<success_criteria>
The direct-manipulation baseline covers pan, zoom, marquee select, multi-select, drag, resize, and rotate with predictable state transitions and viewport persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canvas-core-authoring/01-02-SUMMARY.md`
</output>
