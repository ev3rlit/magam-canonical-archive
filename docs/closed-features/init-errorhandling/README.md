# magam Error Handling & Project Setup

## Overview

magam는 코드 기반 시스템으로, 사용자와 AI가 작성한 `.tsx` 파일에서 다양한 에러가 발생할 수 있습니다. 본 문서는 **프로젝트 자동 세팅**과 **에러 처리 전략**을 정의합니다.

CLI 실행 시 자동으로 TypeScript 설정과 타입 정의를 설치하여, 일반 사용자도 에디터에서 실시간 타입 체크와 자동완성을 사용할 수 있습니다.

## Design Principles

| 원칙 | 설명 |
|------|------|
| Zero Config | 사용자는 `.tsx` 파일만 작성, 설정은 CLI가 자동 생성 |
| 에디터 친화적 | VS Code 등에서 타입 체크, 자동완성 지원 |
| 현상 유지 | 에러 발생 시 마지막 성공 상태 유지 |
| AI 친화적 | MCP 도구로 에러 정보 조회 가능 |

---

## Project Auto Setup

### 사용자 경험

```bash
# 1. 아무 폴더에서
cd ~/my-diagrams

# 2. 파일 만들고
echo 'import { Canvas } from "magam"
export default function() { return <Canvas /> }' > overview.tsx

# 3. CLI 실행
npx @magam/cli dev

# 4. CLI가 자동으로 설정
✓ Setting up magam environment...
✓ tsconfig.json created
✓ Type definitions installed
🚀 magam running at http://localhost:3000
📁 Watching: ~/my-diagrams
```

VS Code에서 바로 타입 체크 + 자동완성 동작.

### 자동 생성 파일 구조

```
사용자 폴더/
├── overview.tsx           ← 사용자 파일
├── architecture.tsx       ← 사용자 파일
├── components/
│   └── api-layer.tsx      ← 사용자 파일
│
├── tsconfig.json          ← 자동 생성 (에디터 인식용)
├── .gitignore             ← 자동 생성/수정
└── .magam/           ← 자동 생성 (캐시 폴더)
    ├── node_modules/
    │   └── magam/    ← 타입 정의 복사
    │       ├── index.d.ts
    │       └── package.json
    └── .gitignore
```

### 생성 파일 상세

**tsconfig.json**

에디터가 TypeScript 프로젝트로 인식하도록 합니다.

```json
{
  "compilerOptions": {
    "jsx": "react-jsx",
    "module": "ESNext",
    "target": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noEmit": true,
    "baseUrl": ".",
    "paths": {
      "magam": ["./.magam/node_modules/magam"]
    }
  },
  "include": ["**/*.tsx"],
  "exclude": [".magam", "node_modules"]
}
```

**.magam/node_modules/magam/index.d.ts**

타입 정의 파일. CLI에 내장된 @magam/core의 타입을 복사합니다.

```typescript
// 자동 생성됨 - 수정하지 마세요
// @magam/cli에서 복사됨

import { ReactNode } from 'react';

export interface CanvasProps {
  width?: number;
  height?: number;
  grid?: boolean;
  gridSize?: number;
  className?: string;
  children?: ReactNode;
}

export interface StickyProps {
  id: string;
  x: number;
  y: number;
  width?: number;
  height?: number;
  className?: string;
  children?: ReactNode;
}

// ... 나머지 컴포넌트 타입
```

**.gitignore**

```
.magam/
```

### 세팅 흐름

```
┌─────────────────────────────────────────────────────────────┐
│                 npx @magam/cli dev                     │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│               .magam/ 폴더 확인                         │
└─────────────────────────┬───────────────────────────────────┘
                          │
            ┌─────────────┴─────────────┐
            ▼                           ▼
     ┌─────────────┐             ┌─────────────┐
     │   있음      │             │   없음      │
     └──────┬──────┘             └──────┬──────┘
            │                           │
            ▼                           ▼
┌─────────────────────┐       ┌─────────────────────┐
│  버전 확인          │       │  .magam/ 생성   │
│  (CLI 버전 != 캐시) │       │  타입 정의 복사      │
└──────────┬──────────┘       │  tsconfig.json 생성  │
           │                  │  .gitignore 수정     │
     ┌─────┴─────┐            └──────────┬──────────┘
     ▼           ▼                       │
┌─────────┐ ┌─────────┐                  │
│ 동일    │ │ 다름    │                  │
│ Skip    │ │ 재생성  │                  │
└────┬────┘ └────┬────┘                  │
     │           │                       │
     └─────┬─────┴───────────────────────┘
           ▼
┌─────────────────────────────────────────────────────────────┐
│                     서버 시작                                │
└─────────────────────────────────────────────────────────────┘
```

### 에디터 지원 기능

| 기능 | 설명 |
|------|------|
| 타입 체크 | 잘못된 props에 빨간 밑줄 |
| 자동완성 | `<Sticky ` 입력 시 props 목록 표시 |
| 호버 문서 | props 위에 마우스 올리면 설명 표시 |
| Go to Definition | 컴포넌트 클릭 시 타입 정의로 이동 |
| Import 자동완성 | `Canvas` 입력 시 import 문 자동 추가 |

### CLI 업데이트 시

CLI 버전이 올라가면 `.magam/` 폴더를 재생성합니다.

```bash
npx @magam/cli dev

# CLI 버전 변경 감지
✓ Updating magam environment (v0.1.0 → v0.2.0)...
✓ Type definitions updated
🚀 magam running at http://localhost:3000
```

---

## Error Types

### 1. Syntax Error (문법 에러)

트랜스파일 단계에서 발생합니다.

| 예시 | 설명 |
|------|------|
| `<Sticky id="api"` | 닫는 태그 누락 |
| `<Sticky id={api}>` | 따옴표 누락 |
| `export default function() {` | 잘못된 문법 |

```tsx
// 에러 코드
<Sticky id="api" x={100} y={100}
  API Server
</Sticky>

// 에러 메시지
Syntax Error: Unexpected token (line 3)
```

### 2. Props Error (속성 에러)

렌더링 단계에서 필수 props 누락 또는 잘못된 타입 감지.

| 예시 | 설명 |
|------|------|
| `<Sticky>` | id, x, y 누락 |
| `<Sticky id="api" x="100">` | x가 string (number여야 함) |
| `<Shape id="s" x={0} y={0}>` | shape 누락 |

```tsx
// 에러 코드
<Sticky x={100} y={100}>
  API Server
</Sticky>

// 에러 메시지
Props Error: Missing required prop 'id' on Sticky
```

### 3. Reference Error (참조 에러)

존재하지 않는 id를 참조할 때 발생.

| 예시 | 설명 |
|------|------|
| `<Edge to="없는-id" />` | 존재하지 않는 노드 참조 |
| `<Node parentId="없는-id">` | 존재하지 않는 부모 참조 |

```tsx
// 에러 코드
<Sticky id="api" x={100} y={100}>
  API
  <Edge to="database" />  // database가 없음
</Sticky>

// 에러 메시지
Reference Error: Edge target 'database' not found
```

### 4. Structure Error (구조 에러)

컴포넌트를 잘못된 위치에서 사용할 때 발생.

| 예시 | 설명 |
|------|------|
| `<Node>` 단독 사용 | MindMap 밖에서 Node 사용 |
| `<Canvas>` 중첩 | Canvas 안에 Canvas |
| `<Edge>` 루트 레벨 자식 | from 없이 Canvas 직접 자식으로 Edge |

```tsx
// 에러 코드
<Canvas>
  <Node id="root">Root</Node>  // MindMap 없이 Node 사용
</Canvas>

// 에러 메시지
Structure Error: Node must be inside MindMap
```

### 5. Import Error (Import 에러)

존재하지 않는 컴포넌트를 import할 때 발생.

| 예시 | 설명 |
|------|------|
| `import { Foo } from 'magam'` | 없는 컴포넌트 |
| `import { Canvas } from 'other-lib'` | 다른 라이브러리 |

```tsx
// 에러 코드
import { Canvas, Sticky, Button } from 'magam'

// 에러 메시지
Import Error: 'Button' is not exported from 'magam'
```

---

## Error Handling Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    파일 변경 감지                            │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    트랜스파일 시도                           │
│                    (esbuild)                                │
└─────────────────────────┬───────────────────────────────────┘
                          │
            ┌─────────────┴─────────────┐
            ▼                           ▼
     ┌─────────────┐             ┌─────────────┐
     │   성공      │             │   실패      │
     └──────┬──────┘             └──────┬──────┘
            │                           │
            ▼                           ▼
┌─────────────────────┐       ┌─────────────────────┐
│   렌더링 시도       │       │   Syntax Error 저장 │
│   (React 실행)      │       │   현상 유지         │
└──────────┬──────────┘       └─────────────────────┘
           │
     ┌─────┴─────┐
     ▼           ▼
┌─────────┐ ┌─────────┐
│  성공   │ │  실패   │
└────┬────┘ └────┬────┘
     │           │
     ▼           ▼
┌──────────┐ ┌─────────────────────┐
│ 새 상태  │ │ Props/Reference/    │
│ 렌더링   │ │ Structure Error 저장│
└──────────┘ │ 현상 유지           │
             └─────────────────────┘
```

---

## Error Storage

에러는 메모리에 저장되어 MCP와 뷰어에서 조회 가능합니다.

```typescript
interface ErrorStore {
  errors: MagamError[]
  lastSuccessState: CanvasState
  lastSuccessTime: string
}

interface MagamError {
  type: 'syntax' | 'props' | 'reference' | 'structure' | 'import'
  file: string
  line: number
  column: number
  message: string
  code: string           // 해당 라인 코드
  suggestion?: string    // 수정 제안
}
```

---

## MCP Interface

### canvas.getErrors()

현재 에러 목록을 조회합니다.

```typescript
// 요청
canvas.getErrors()

// 응답
{
  errors: [
    {
      type: "props",
      file: "architecture.tsx",
      line: 15,
      column: 5,
      message: "Missing required prop 'id' on Sticky",
      code: "    <Sticky x={100} y={100}>",
      suggestion: "Add id prop: <Sticky id=\"unique-id\" x={100} y={100}>"
    }
  ],
  hasErrors: true,
  lastSuccessTime: "2024-01-15T10:30:00Z"
}
```

### 에러 없을 때

```typescript
{
  errors: [],
  hasErrors: false,
  lastSuccessTime: "2024-01-15T10:35:00Z"
}
```

---

## AI Workflow

AI가 코드 수정 후 에러를 자동으로 확인하고 수정하는 흐름입니다.

```
사용자: "architecture 페이지에 Cache 노드 추가해줘"

AI:
1. code.read("architecture") → 현재 코드 확인
2. 코드 수정
3. code.write(newCode, "architecture") → 저장
4. canvas.getErrors() → 에러 확인
   ├── 에러 없음 → "Cache 노드를 추가했습니다" 보고
   └── 에러 있음 → 에러 내용 확인 후 자동 수정 시도
5. (에러 시) 수정 후 다시 code.write()
6. canvas.getErrors() → 재확인
7. 완료 보고
```

### 예시 대화

```
AI: Cache 노드를 추가하겠습니다.

[code.write 실행]
[canvas.getErrors 실행]

AI: 에러가 발생했습니다. Edge 대상 'database'가 존재하지 않네요.
    database 노드를 먼저 추가할까요, 아니면 다른 노드에 연결할까요?

사용자: db 노드에 연결해줘

[code.write 실행]
[canvas.getErrors 실행]

AI: 수정 완료했습니다. Cache 노드가 db 노드에 연결되었습니다.
```

---

## User Display

### Terminal (CLI)

상세한 에러 정보를 표시합니다.

```
[magam] Watching ~/my-diagrams

[magam] ✓ overview.tsx loaded
[magam] ✓ architecture.tsx loaded

[magam] File changed: architecture.tsx
[magam] ❌ Error in architecture.tsx

  Props Error: Missing required prop 'id' on Sticky

    14 |   <Canvas>
    15 |     <Sticky x={100} y={100}>
       |     ^^^^^^^
    16 |       API Server

  💡 Suggestion: Add id prop
     <Sticky id="unique-id" x={100} y={100}>

[magam] Keeping previous state
```

### Web Viewer

간단한 상태 표시와 에러 요약을 보여줍니다.

**정상 상태**

```
┌──────────┬──────────────────────────────────────┐
│ Pages    │                           🔄 Synced │
├──────────┼──────────────────────────────────────┤
│ overview │                                      │
│ architect│           캔버스                     │
│          │                                      │
├──────────┴──────────────────────────────────────┤
│ Selected: (none)                                │
└─────────────────────────────────────────────────┘
```

**에러 상태**

```
┌──────────┬──────────────────────────────────────┐
│ Pages    │                           ⚠️ Error  │
├──────────┼──────────────────────────────────────┤
│ overview │                                      │
│ architect│     (마지막 성공 상태 유지)          │
│    ⚠️    │                                      │
├──────────┴──────────────────────────────────────┤
│ ❌ architecture.tsx:15 - Missing required 'id'  │
└─────────────────────────────────────────────────┘
```

- 헤더: `⚠️ Error` 상태 표시
- 사이드바: 에러 있는 파일에 ⚠️ 아이콘
- 하단 바: 에러 메시지 한 줄 요약
- 캔버스: 마지막 성공 상태 유지

### 에러 상세 보기

하단 바 클릭 시 에러 상세 패널 확장

```
┌──────────┬──────────────────────────────────────┐
│ Pages    │                           ⚠️ Error  │
├──────────┼──────────────────────────────────────┤
│ overview │                                      │
│ architect│     (마지막 성공 상태 유지)          │
│    ⚠️    │                                      │
├──────────┴──────────────────────────────────────┤
│ ❌ Errors (1)                              [×]  │
├─────────────────────────────────────────────────┤
│ architecture.tsx:15:5                           │
│ Props Error: Missing required prop 'id'         │
│                                                 │
│   <Sticky x={100} y={100}>                      │
│   ^^^^^^^                                       │
│                                                 │
│ 💡 Add id prop: <Sticky id="..." x={100}...     │
└─────────────────────────────────────────────────┘
```

---

## Error Suggestions

가능한 경우 수정 제안을 제공합니다.

| 에러 유형 | 제안 |
|----------|------|
| Missing prop 'id' | `Add id prop: <Sticky id="unique-id" ...>` |
| Missing prop 'x' | `Add position: x={0}` |
| Invalid prop type | `Change to number: x={100} instead of x="100"` |
| Reference not found | `Available nodes: api, database, cache` |
| Node outside MindMap | `Wrap with MindMap: <MindMap><Node>...</Node></MindMap>` |
| Unknown import | `Available: Canvas, Sticky, Shape, Text, Edge, Group, MindMap, Node` |

---

## Summary

| 항목 | 설명 |
|------|------|
| 에러 시 동작 | 마지막 성공 상태 유지 |
| 에러 저장 | 메모리 (ErrorStore) |
| MCP 도구 | canvas.getErrors() |
| 터미널 표시 | 상세 에러 + 코드 + 제안 |
| 뷰어 표시 | 상태 아이콘 + 요약 + 상세 패널 |
| AI 워크플로우 | 자동 에러 확인 및 수정 시도 |